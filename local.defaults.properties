# Default API keys for development (checked into git)
# These are placeholder values - actual keys should be in secrets.properties (gitignored)
# The Secrets Gradle Plugin will use secrets.properties if it exists, otherwise this file
MAPS_API_KEY=PLACEHOLDER_REPLACE_WITH_REAL_KEY
